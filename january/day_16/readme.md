# Day 17 – Design HashSet

Design a HashSet without using built-in hash set libraries.

Methods:

- add(key)
- remove(key)
- contains(key)

Approach:

- Use an array to store unique values

Time Complexity:

- add / contains / remove: O(n)

Space Complexity: O(n)

LeetCode: Design HashSet (Easy)
